export const baseUrl = "http://localhost:8000/api/v1/"
// export const baseUrl = "https://skool-backend.onrender.com/api/v1/"

export const extractFirstLetter = (word) => {
    return word.charAt(0).toUpperCase();
}